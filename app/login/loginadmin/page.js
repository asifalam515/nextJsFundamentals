export const generateMetadata = async ({ params }) => {
  return {
    title: "Admin Login",
  };
};
const LoginAdmin = () => {
  return (
    <div>
      <h1 className="text-4xl">Login Admin</h1>
    </div>
  );
};

export default LoginAdmin;
